===============
Marking Up Text
===============

     This paragraph will result in an *indented block*, typically ``used for quoting`` other text.

This is another **bold**.

This is *italic*. 

This is a :subscript:`subscript text`.

This is a :superscript:`superscript text`.

This is :code:`code of some sort`.

 .. centered::
     This text is centered across page via the :code:`centered` directive.

Press :kbd:`ENTER` on the keyboard. Press :kbd:`Ctrl` + :kbd:`Alt`. This is done using the :code:`kbd` role.

When instructing the reader to navigate through some menus, use the :code:`menuselection` role. Example: Select :menuselection:`Administration --> Management --> Server`. 